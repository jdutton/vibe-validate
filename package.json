{
  "name": "vibe-validate",
  "version": "0.13.0",
  "type": "module",
  "private": true,
  "description": "Git-aware validation orchestration for vibe coding (LLM-assisted development)",
  "keywords": [
    "validation",
    "git",
    "cache",
    "llm",
    "ai",
    "agent",
    "typescript",
    "testing",
    "ci",
    "pre-commit"
  ],
  "author": "Jeff Dutton",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jdutton/vibe-validate.git"
  },
  "bugs": {
    "url": "https://github.com/jdutton/vibe-validate/issues"
  },
  "homepage": "https://github.com/jdutton/vibe-validate#readme",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "find packages -name '*.tsbuildinfo' -delete && pnpm -r build",
    "build:clean": "pnpm -r clean && pnpm build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint \"packages/*/src/**/*.ts\" --max-warnings 0",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "dev": "pnpm -r --parallel dev",
    "validate": "node packages/cli/dist/bin.js validate",
    "pre-commit": "node packages/cli/dist/bin.js pre-commit",
    "state": "node packages/cli/dist/bin.js state",
    "history": "node packages/cli/dist/bin.js history",
    "cleanup": "node packages/cli/dist/bin.js cleanup",
    "sync-check": "node packages/cli/dist/bin.js sync-check",
    "watch-pr": "node packages/cli/dist/bin.js watch-pr",
    "generate-workflow": "node packages/cli/dist/bin.js generate-workflow",
    "generate-cli-docs": "node tools/generate-cli-docs.js",
    "pre-publish": "node tools/pre-publish-check.js",
    "publish:dry-run": "pnpm -r --filter='@vibe-validate/*' exec npm publish --dry-run",
    "publish:extractors": "cd packages/extractors && pnpm publish --no-git-checks",
    "publish:git": "cd packages/git && pnpm publish --no-git-checks",
    "publish:config": "cd packages/config && pnpm publish --no-git-checks",
    "publish:core": "cd packages/core && pnpm publish --no-git-checks",
    "publish:cli": "cd packages/cli && pnpm publish --no-git-checks",
    "publish:umbrella": "cd packages/vibe-validate && pnpm publish --no-git-checks",
    "publish:all": "npm run pre-publish && npm run publish:extractors && npm run publish:git && npm run publish:config && npm run publish:core && npm run publish:cli && npm run publish:umbrella",
    "version:patch": "pnpm -r exec npm version patch --no-git-tag-version",
    "version:minor": "pnpm -r exec npm version minor --no-git-tag-version",
    "version:major": "pnpm -r exec npm version major --no-git-tag-version",
    "release:patch": "npm run version:patch && npm run build && git add -A && git commit -m 'chore: Release patch version' && npm run tag:create",
    "release:minor": "npm run version:minor && npm run build && git add -A && git commit -m 'chore: Release minor version' && npm run tag:create",
    "release:major": "npm run version:major && npm run build && git add -A && git commit -m 'chore: Release major version' && npm run tag:create",
    "tag:create": "node -e \"const pkg = require('./packages/cli/package.json'); const tag = 'v' + pkg.version; require('child_process').execSync('git tag -a ' + tag + ' -m \\\"Release ' + tag + '\\\"', {stdio: 'inherit'});\"",
    "docs:build": "typedoc",
    "docs:serve": "cd docs/api && python3 -m http.server 8000",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "@typescript-eslint/eslint-plugin": "^8.46.1",
    "@typescript-eslint/parser": "^8.46.1",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.37.0",
    "husky": "^9.1.7",
    "typedoc": "^0.28.14",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  }
}
