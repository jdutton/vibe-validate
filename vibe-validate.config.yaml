validation:
  phases:
    - name: Pre-Qualification
      parallel: false
      steps:
        - name: Resource Link Validation
          command: pnpm --filter vibe-validate exec vat resources validate
        - name: Build All Packages (TypeScript)
          command: pnpm exec turbo run build
        - name: Repository Structure Validation
          command: pnpm exec tsx packages/dev-tools/src/validate-repo-structure.ts
          description: 'Validates build system, package.json, and source file conventions'
        - name: TypeScript Type Check
          command: pnpm exec turbo run typecheck
        - name: ESLint Code Quality
          command: pnpm lint
        - name: Code Duplication Check
          command: pnpm duplication-check
    - name: Testing
      parallel: false
      steps:
        - name: Unit Tests with Coverage
          command: pnpm test:coverage
        - name: Integration & System Tests
          command: pnpm test:integration
git:
  mainBranch: main
hooks:
  preCommit:
    enabled: true
    secretScanning:
      enabled: true
ci:
  nodeVersions:
    - '22'
    - '24'
  os:
    - ubuntu-latest
    - windows-latest
  dependencyLockCheck:
    runOn: validate
developerFeedback: true
